/** @type {import('next-sitemap').IConfig} */
module.exports = {
	siteUrl: 'https://rahulcodepython.in',  // Change to your domain
	generateRobotsTxt: true, // Generates a robots.txt file
	sitemapSize: 5000, // Optional: Split sitemap if you have many pages
};
